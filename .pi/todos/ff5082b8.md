{
  "id": "ff5082b8",
  "title": "Show installed model status and local load/unload actions",
  "tags": [
    "ui",
    "models",
    "swift"
  ],
  "status": "closed",
  "created_at": "2026-03-06T00:44:51.469Z"
}

Implemented a unified Models list UI. Installed models now show explicit Installed/Loaded badges and expose inline Load/Unload actions directly in the same row, including manually imported local models. Added `LLMService.isActive(_:)` to let the UI detect the currently loaded model.

Files changed:
- `LocalCoder/Views/ModelManagerView.swift`
- `LocalCoder/Services/LLMService.swift`

Validation:
- `xcodebuild -project LocalCoder.xcodeproj -scheme LocalCoder -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build` ✅
- Physical device build attempted via ios-device-build skill, but code signing is not configured with a development team, so install/run on device is still blocked.
